# AutoParts Express - Variables de entorno
# Copia este archivo a .env y ajusta los valores:
#   cp .env.example .env
# Luego carga las variables antes de ejecutar:
#   export $(cat .env | xargs)

DB_HOST=localhost
DB_PORT=5432
DB_NAME=autoparts_db
DB_USER=postgres
DB_PASSWORD=M@ichelsboy@07
